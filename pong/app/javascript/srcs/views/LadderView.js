import common from '../common';
import template from '../templates/LadderView.html';

const LadderView = common.View.extend({
  template,
  events: {},
  onInitialize() {},
  onRender() {},
});

export default LadderView;
