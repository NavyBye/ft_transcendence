import common from '../common';
import template from '../templates/TournamentView.html';

const TournamentView = common.View.extend({
  template,
  events: {},
  onInitialize() {},
  onRender() {},
});

export default TournamentView;
