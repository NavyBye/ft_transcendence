import common from '../common';
import template from '../templates/BannedView.html';

const BannedView = common.View.extend({
  el: '#content',
  template,
  onInitialize() {},
  onRender() {},
});

export default BannedView;
