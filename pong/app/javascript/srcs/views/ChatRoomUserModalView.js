import common from '../common';
import template from '../templates/ChatRoomUserModal.html';

const ChatRoomUserModalView = common.View.extend({
  template,
  events: {},
});

export default ChatRoomUserModalView;
