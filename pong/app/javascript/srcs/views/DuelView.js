import common from '../common';
import template from '../templates/DuelView.html';

const DuelView = common.View.extend({
  template,
  events: {},
  onInitialize() {},
  onRender() {},
});

export default DuelView;
